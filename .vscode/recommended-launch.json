{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "All Unit Tests",
            "type": "go",
            "request": "launch",
            "mode": "test",
            "program": "${workspaceFolder}/tests/unit",
            "args": ["-test.timeout=3m"]
        }
    ]
}